//import styles from '../../flexboxgrid.global.css';

//console.log('styles: ', styles)

export default function getClass(className) {
  //console.log('getClass: ', (styles && styles[className]) ? styles[className] : className)
  return className;
}
